# Deprecated Mirror

`.agents/` is being retained as a compatibility mirror while the project migrates to `codex/`.

Prefer `codex/` for new agent assets:

- `codex/skills/`
- `codex/rules/`
- `codex/context/`
- `codex/workflows/`

Do not remove `.agents/` until migration parity has been verified.
